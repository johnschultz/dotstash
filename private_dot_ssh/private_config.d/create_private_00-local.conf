# Machine-local SSH aliases. chezmoi created this file once and will not overwrite it.
# Real hostnames belong here, not in the public repo. An alias only needs a HostName:
# 20-work.conf matches the resolved host and supplies user, key, proxy and keepalives.
#
# Host mydesk
#   HostName dev-dsk-<alias>-<id>.<region>.amazon.com
