[user]
    name = Johnathon Schultz
    email = schultjo@twitch.tv
    signingkey = 0x3AB557CAC5BF3B06

[core]
    editor = nvim
    whitespace = trailing-space,space-before-tab,tab-in-indent

[alias]
    ap = add -p
    gl = log --graph --decorate --pretty=oneline --abbrev-commit --all --date-order
    glb = log --graph --decorate --pretty=oneline --abbrev-commit --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
    gla = !trap "" 13 && git log --graph --oneline --decorate --all $(git reflog | awk '{print $1}')
    b = branch
    d = diff
    dc = diff --cached
    st = status -sb
    ci = commit
    co = checkout
    up = pull --rebase --autostash
    ff = merge --ff-only
  purr = pull --rebase --autostash
  mend = commit --amend --no-edit
  mendedit = commit --amend


[merge]
    stat = False
    tool = vimdiff

#[mergetool "kdiff3"]
#    path = /Users/pkoch/Applications/kdiff3.app/Contents/MacOS/kdiff3

[color]
    ui = auto

[color "branch"]
    current = green reverse
    local = green
    remote = red

[push]
    default = current

[grep]
    lineNumber = true

[mergetool]
    prompt = false

[rerere]
    enabled = true
    autoupdate = true

[rebase]
    autosquash = true
[commit]
	gpgsign = true
	template = /Users/schultjo/.git-commit-template.txt
	cleanup = strip
[gpg]
	program = gpg
[url "https://code.justin.tv:"]
	insteadOf = git@code.justin.tv:
[pull]
	rebase = true
